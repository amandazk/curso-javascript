// Some 500 ao valor de scroll abaixo,
// atribuindo o novo valor ao scroll
var scroll = 1000;
scroll += 500;
console.log(scroll);

// Atribua true para a variável darCredito,
// caso o cliente possua carro e caso.
// False caso o contrário
var possuiCarro = true;
var possuiCasa = true;
var darCredito = (possuiCarro && possuiCasa);
console.log(darCredito);
// quando o retorno for booleano, não preciso escrever o código completo
